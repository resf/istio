name: istio-base-istio-distroless
"on":
  push:
    paths:
    - .github/workflows/istio-base-istio-distroless.yml
    - build/istio-base/Dockerfile.istio-distroless
    - build/istio-base/Dockerfile.version
    - build/istio-base/Makefile
jobs:
  istio-distroless:
    runs-on:
    - ubuntu-latest
    defaults:
      run:
        working-directory: build/istio-base
    steps:
    - uses: actions/checkout@v2
    - uses: actions/cache@v2
      with:
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        path: /tmp/.buildx-cache
        restore-keys: ${{ runner.os }}-buildx-
    - uses: docker/setup-qemu-action@v1
    - uses: docker/setup-buildx-action@v1
    - uses: docker/login-action@v1
      with:
        password: ${{ secrets.DOCKER_PASSWORD }}
        username: ${{ secrets.DOCKER_USERNAME }}
    - name: Versioned Build
      if: github.ref == 'refs/heads/master'
      run: make build HUB=querycapistio NAME=istio-distroless
    - name: Temp Build
      if: github.ref != 'refs/heads/master'
      run: make build TAG=temp-${{ github.sha }} HUB=querycapistio NAME=istio-distroless
  sync-istio-distroless:
    runs-on:
    - self-hosted
    - arm64
    needs:
    - istio-distroless
    if: github.ref == 'refs/heads/master'
    defaults:
      run:
        working-directory: build/istio-base
    steps:
    - uses: actions/checkout@v2
    - uses: docker/setup-qemu-action@v1
    - uses: docker/setup-buildx-action@v1
    - uses: docker/login-action@v1
      with:
        password: ${{ secrets.DOCKER_MIRROR_PASSWORD }}
        registry: ${{ secrets.DOCKER_MIRROR_REGISTRY }}
        username: ${{ secrets.DOCKER_MIRROR_USERNAME }}
    - run: |-
        for TAG in $(make image HUB=querycapistio NAME=istio-distroless)
        do
            docker buildx build --push --platform linux/arm64,linux/amd64 --tag ${{ secrets.DOCKER_MIRROR_REGISTRY }}/${TAG} --build-arg TAG=${TAG} --file ../Dockerfile.sync .
        done
